# Este arquivo foi substituído pelo pacote config/settings/.
# Remova-o manualmente se o seu sistema de arquivos permitir.
# O Django carregará config.settings.development (ou production)
# conforme definido em DJANGO_SETTINGS_MODULE.
raise ImportError(
    "Use config.settings.development ou config.settings.production, "
    "não config.settings diretamente."
)
